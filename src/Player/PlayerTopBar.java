package src.Player;
import javafx.scene.input.MouseEvent;
import src.auth.Logout;
import java.io.IOException;

public class PlayerTopBar extends Logout {
    public void logout(MouseEvent event) throws IOException {
        super.logout(event);
    }
}
